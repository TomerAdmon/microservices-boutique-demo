spec_version: 2
description: network best practice for aws based applications


outputs:
  private_subnet_cidrs:
    value: '{{ .grains.network.outputs.private_subnet_cidrs }}'
  public_subnet_cidrs:
    value: '{{ .grains.network.outputs.public_subnet_cidrs }}'
  vpc_cidr:
    value: '{{ .grains.network.outputs.vpc_cidr }}'
grains:
  network:
    kind: terraform
    spec:
      tags:
        auto-tag: false
      source:
        store: microservices-boutique-demo
        path: terraform/network
      agent:
      # The Torque agent that will be used to provision the environment.
        name: demo-dev
        service-account: torque-dev-sa
      inputs:
      - availability_zones: '["us-east-1a", "us-east-1b"]'
      - region: us-east-1
      env-vars: []
      outputs:
      - private_subnet_cidrs
      - public_subnet_cidrs
      - vpc_cidr
    # The terraform version that will be used to deploy the module
    tf-version: 1.5.5
